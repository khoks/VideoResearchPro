# Web framework
fastapi>=0.115.9
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20

# Database
sqlalchemy>=2.0.40
alembic>=1.15.0
aiosqlite>=0.21.0

# Async
celery[redis]>=5.5.0
redis>=5.3.0

# YouTube
google-api-python-client>=2.169.0
youtube-transcript-api>=1.0.0
yt-dlp>=2024.1.1

# LLM / Agents
openai>=1.78.0
langchain>=0.3.25
langchain-openai>=0.3.18
langgraph>=0.4.0
tiktoken>=0.9.0

# Vector DB
chromadb>=1.0.7
sentence-transformers>=4.1.0

# Templating
jinja2>=3.1.6

# Config
pydantic-settings>=2.9.0

# CORS
python-dotenv>=1.1.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0
email-validator>=2.0.0
# T-5.4.6 MFA / TOTP — RFC 6238 generator + verifier + provisioning URI.
pyotp>=2.9.0

# HTTP client (used by social-media connectors and article extraction)
httpx>=0.27.0

# Article extraction (E-1.6 T-1.6.1) — trafilatura is the primary path
# (high-quality boilerplate-removal for most static pages). Playwright
# fallback for SPAs (T-1.6.1 follow-up — needs browser-binary install
# and is gated on operator opt-in to keep the default install lean).
trafilatura>=1.12.0

# Podcast connector (M-1.7 / E-1.7) — feedparser is the de-facto RSS /
# Atom feed parser for Python. Handles the dozens of podcast-feed
# variations (iTunes namespace extensions, Atom-vs-RSS-2.0, malformed
# tags) that a hand-rolled parser would have to relitigate.
feedparser>=6.0.10

# PDF / e-book connector (M-1.8 / E-1.8) — PyMuPDF (also imported as
# `fitz`) handles text + table extraction with per-page boundaries
# preserved. AGPL-licensed; if a future commercial path needs a more
# permissive license we can swap to pdfminer.six (slower, fewer
# features) or pypdf (very limited table support).
pymupdf>=1.24.0
