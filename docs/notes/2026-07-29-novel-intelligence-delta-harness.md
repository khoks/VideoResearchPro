---
status: raw — do not edit
date: 2026-07-29
context: Seed message for invention N-001 (production-replay blind-delta harness). Preserved verbatim for prior-art chronology.
---

# Verbatim user message — 2026-07-29

> I want you to create and use Fable 5 agents in this Claude session to process the same functions/prompts in this app for the same data(the same 200 videos assumimg search returned and selected the same list, apart from running the search + selection functionality as well on the original job criteria) which was there in the AI topic job across the same set of functions/prompts, then analyze the qualitative results comparing them with your default choices in this app across all the functions/prompts, and then tell me how far off are the smaller/less-thinking non Fable5 choices. The goal here is to determine that how much intelligence are we loosing when we compare the final result and intermediate results when Fable 5 with max thinking is used across all steps vs defaults which we have set in the app. When you are doing this, just spawn the agents like you do in the code session in desktop and give them goals and data sources and promting instructions to work on and use, so that you don't use the anthropic API key for evaluating fable5 but rather use theclaude code desktop app subscription here to do the same.

## Follow-up constraint — same session

> the usage limit for the 5 hour window had exhausted for this claude code session. Now its available again. Fable 5 weekly usage limit is 70% utilized. so if during completion of your remaining work, fable 5 usage limit exhausts, then switch to opus 5 in this claude code session, or if not possible at your end then ask me so that I can switch it from here in the desktop app controls in this session.

(Later in the session the user switched the session model to `claude-opus-5` via `/model`, making the challenger fleet mixed-provenance — recorded as a caveat in D-055.)
